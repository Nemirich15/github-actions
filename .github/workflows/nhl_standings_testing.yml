name: Testing Standings page
on: workflow_dispatch
jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: run testing
        uses: actions/checkout@v4
      - name: Install dependecies
        run: nmp ci
      - name: Test applications
        run: npm run wdio ./wdio test/specs/standings_page.js
