name: Testing NHL Workflow
on: workflow_dispatch
jobs:
 print: 
   runs-on: windows-latest
   steps:
    - name: run testing
      run: actions/checkout@v4
    - name: Install dependecies
      run: npm ci
    - name: Test application
      run: npm run wdio ./wdio github/worcflows/nhl_testing.yml
